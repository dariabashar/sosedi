# 🔗 Интеграция Flutter с Node.js Backend

## ✅ Что мы создали:

### **1. API Сервис (`lib/services/api_service.dart`)**
- Полный набор методов для работы с бэкендом
- Автоматическая аутентификация через Firebase
- Обработка ошибок и статусов

### **2. Тестовый экран (`lib/screens/api_test_screen.dart`)**
- Проверка подключения к бэкенду
- Тестирование создания и загрузки постов
- Визуальная индикация статуса

### **3. Обновленный main.dart**
- Проверка бэкенда при запуске приложения
- Кнопка для перехода к тестированию API

## 🚀 Как использовать:

### **1. Запуск бэкенда:**
```bash
cd sosedi-backend
node full-server.js
```

### **2. Запуск Flutter приложения:**
```bash
cd sosedi-main
flutter run
```

### **3. Тестирование интеграции:**
1. Войдите в приложение (любой способ аутентификации)
2. Нажмите кнопку **"Тест API"**
3. Проверьте статус бэкенда
4. Попробуйте создать и загрузить посты

## 📱 Доступные API методы:

### **Пользователи:**
- `getUserProfile()` - получить профиль
- `createUser()` - создать пользователя

### **Посты:**
- `getNearbyPosts()` - получить посты поблизости
- `createPost()` - создать пост

### **Группы:**
- `getNearbyGroups()` - получить группы поблизости
- `createGroup()` - создать группу

### **Объявления:**
- `getNearbyAdvertisements()` - получить объявления
- `createAdvertisement()` - создать объявление

### **События:**
- `getNearbyEvents()` - получить события
- `createEvent()` - создать событие

### **Чаты:**
- `getUserChats()` - получить чаты пользователя
- `sendMessage()` - отправить сообщение

## 🔧 Настройка для продакшена:

### **1. Измените URL в ApiService:**
```dart
// Для локальной разработки
static const String baseUrl = 'http://localhost:3000/api';

// Для продакшена
static const String baseUrl = 'https://your-domain.com/api';
```

### **2. Для Android эмулятора:**
```dart
static const String baseUrl = 'http://10.0.2.2:3000/api';
```

### **3. Для физического устройства:**
```dart
static const String baseUrl = 'http://YOUR_COMPUTER_IP:3000/api';
```

## 🐛 Отладка:

### **Проверка подключения:**
```dart
final isAvailable = await ApiService.checkHealth();
print('Backend available: $isAvailable');
```

### **Логи в консоли:**
- Все API запросы логируются
- Ошибки выводятся в консоль
- Статус подключения отображается в UI

## 📋 Следующие шаги:

1. **Протестируйте все API методы**
2. **Интегрируйте в существующие экраны**
3. **Добавьте обработку ошибок**
4. **Настройте кэширование**
5. **Добавьте индикаторы загрузки**

## 🎯 Готово к использованию!

Ваше Flutter приложение теперь полностью интегрировано с Node.js бэкендом! 