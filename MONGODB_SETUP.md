# Настройка MongoDB Atlas

## 🚀 Быстрый старт с MongoDB Atlas

### 1. Создание аккаунта
1. Перейдите на [MongoDB Atlas](https://www.mongodb.com/atlas)
2. Нажмите "Try Free"
3. Заполните форму регистрации

### 2. Создание кластера
1. Выберите "Build a Database"
2. Выберите "FREE" план (M0)
3. Выберите провайдера (AWS/Google Cloud/Azure)
4. Выберите регион (ближайший к вам)
5. Нажмите "Create"

### 3. Настройка безопасности
1. **Database Access** - создайте пользователя:
   - Username: `sosedi_admin`
   - Password: `your_secure_password`
   - Role: `Atlas admin`

2. **Network Access** - разрешите доступ:
   - Нажмите "Add IP Address"
   - Выберите "Allow Access from Anywhere" (0.0.0.0/0)
   - Или добавьте ваш IP

### 4. Получение строки подключения
1. Нажмите "Connect"
2. Выберите "Connect your application"
3. Скопируйте строку подключения

## 🔧 Настройка в проекте

### 1. Обновить .env файл
```bash
# Добавить в .env
MONGODB_URI=mongodb+srv://sosedi_admin:your_password@cluster0.xxxxx.mongodb.net/sosedi?retryWrites=true&w=majority
```

### 2. Запуск сервера с MongoDB
```bash
node src/server.js
```

## 🧪 Тестирование подключения

### 1. Проверка в MongoDB Compass
1. Скачайте [MongoDB Compass](https://www.mongodb.com/products/compass)
2. Подключитесь используя строку подключения
3. Создайте базу данных `sosedi`

### 2. Тестирование через API
```bash
# Создать пользователя
curl -X POST http://localhost:3000/api/users/profile \
  -H "Content-Type: application/json" \
  -d '{
    "firebaseUid": "test_user_123",
    "firstName": "Иван",
    "lastName": "Иванов",
    "phoneNumber": "+7 999 123-45-67",
    "address": "ул. Ленина, 1",
    "location": {
      "type": "Point",
      "coordinates": [37.6176, 55.7558]
    }
  }'

# Получить пользователей
curl http://localhost:3000/api/users
```

## 📊 Структура базы данных

### Коллекции:
- `users` - пользователи
- `posts` - посты в ленте
- `groups` - группы по интересам
- `advertisements` - объявления
- `events` - события
- `chats` - чаты

### Индексы:
- Геопространственные индексы для поиска поблизости
- Индексы по дате создания
- Индексы по автору

## 🔒 Безопасность

### Рекомендации:
1. Используйте сильные пароли
2. Ограничьте доступ по IP в продакшене
3. Регулярно обновляйте пароли
4. Используйте переменные окружения

## 🚨 Устранение проблем

### Ошибка подключения:
```bash
# Проверьте строку подключения
echo $MONGODB_URI

# Проверьте доступность
ping cluster0.xxxxx.mongodb.net
```

### Ошибка аутентификации:
- Проверьте username/password
- Убедитесь, что пользователь создан
- Проверьте права доступа

## 📈 Мониторинг

### В MongoDB Atlas:
- Database Performance Advisor
- Real-time metrics
- Slow query analyzer
- Index suggestions 