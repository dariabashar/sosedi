const express = require("express"); const cors = require("cors"); const app = express(); app.use(cors()); app.use(express.json()); app.get("/api/health", (req, res) => { res.json({ success: true, message: "Sosedi API is running!", timestamp: new Date().toISOString() }); }); app.get("/api/test/users", (req, res) => { res.json({ success: true, data: [{ id: "1", firstName: "Иван", lastName: "Иванов", address: "ул. Ленина, 1" }] }); }); app.listen(3000, () => { console.log("🚀 Sosedi Server running on port 3000"); });
